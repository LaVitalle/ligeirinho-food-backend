{
  "name": "ligeirinho-food-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Ligeirinho Food — monorepo de microsserviços (dev). Cada serviço é deployado a partir do seu próprio repositório.",
  "license": "UNLICENSED",
  "scripts": {
    "shared:sync": "node scripts/shared-sync.mjs",
    "split": "node scripts/split-repos.mjs",
    "install:all": "npm --prefix services/gateway install && npm --prefix services/identity install && npm --prefix services/catalog install && npm --prefix services/orders install",
    "typecheck:all": "npm --prefix services/gateway run typecheck && npm --prefix services/identity run typecheck && npm --prefix services/catalog run typecheck && npm --prefix services/orders run typecheck",
    "compose:up": "docker compose up -d --build",
    "compose:down": "docker compose down",
    "compose:logs": "docker compose logs -f"
  }
}
